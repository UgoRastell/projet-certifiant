{% extends 'base.html.twig' %}

{% block title %}Edit Tutoriel{% endblock %}

{% block body %}
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title">Modifier le tutoriel</h2>
                            {{ include('tutoriel/_form.html.twig', {'button_label': 'Update'}) }}
                            {{ include('tutoriel/_delete_form.html.twig') }}
                        <a href="{{ path('app_tutoriel_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
